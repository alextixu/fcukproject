# 專案結構(2026-09-12 重整後)

> **【2026-09-21 警告】這份文件裡的「台灣 50 / tw50」股票池不是臺灣 50 指數的成分股**,而是一份 2026-05-28 手動補齊、沒有日期與出處的 50 檔大型股名單(和任何一天的官方成分股差 7 ~ 18 檔)。**相關數字暫不可引用**;「tw50 等權」對照組用的也是同一份名單,所以「2021 ~ 2025 連續五年輸等權」等結論一併暫停使用。詳見 [2026-09-21_repo的TW50名單不是官方成分股_出處與受影響的結果](2026-09-21_repo的TW50名單不是官方成分股_出處與受影響的結果.md)。

根目錄:`C:\Users\alex\Desktop\專題進度\`。原則:**一個方法一個資料夾,共用資料集中在 `common/`**。

```
專題進度/
├── common/            共用:paths.py(唯一路徑來源)、README.md(快取說明)、cache/(所有資料快取)
├── method_xgb/        主線:240 指標 → XGB → 剪枝(原 xgb_ta_pipeline;v3 模型)
├── method_kline/      系統 A:K 線 AE + OC-SVM,GUI 版(原 KLINE/fcukproject,含 .git)
├── method_chartgcn/   系統 B:Chart-GCN(原 chartgcn,含 .git;快取已移到 common)
├── method_tomt/       Tom 的方法:Qmodel/(唯讀參考)+ port/(逐年重訓移植版)
├── backtest/          跨方法回測:ncku/(成大框架 + run_*.py)、scripts/、report/、results/
├── docs/              週進度紀錄(原 進度整理)
├── _archive/          tomt_backup_20260825、microsoft(Qlib + venv)、K(Chart-GCN 5 月舊副本)、sysmon
└── 101/               空資料夾,用途不明(見 docs/大綱_01)
```

## 路徑對照(舊 → 新)

| 舊 | 新 |
|---|---|
| `xgb_ta_pipeline/` | `method_xgb/`;`experiments/features_*.parquet` → `common/cache/features/` |
| `KLINE/fcukproject/` | `method_kline/` |
| `chartgcn/` | `method_chartgcn/`;`cache/` → `common/cache/yf/`;`cache_2026/` → `common/cache/yf_2026/` |
| `tomt/Qmodel/`、`backtest_2026/tomt_port/` | `method_tomt/Qmodel/`、`method_tomt/port/` |
| `backtest_2026/*.py` | `backtest/scripts/`(回測與比較)、`backtest/report/`(build_*.py、make_report.py、final_template.html) |
| `backtest_2026/ncku/tmp/{official,yf_hist,yf}` | `common/cache/{official,yf_hist,yf_fw}` |
| `backtest_2026/data/kline_raw` | `common/cache/kline_raw` |
| `xgb_ta_pipeline/data/finmind*` | `common/cache/finmind*` |
| `進度整理/` | `docs/` |
| `系統流程架構報告*.pptx` ×3 | 已刪 |

已改路徑並測試通過:`method_xgb/src/{data,chip_data,run_pipeline}.py`、`method_chartgcn/core/data_loader.py`、`method_tomt/port/tomt_walkforward.py`、`backtest/ncku/{run_ncku,fetch_official,make_yf_src,prep_final_v3}.py`、`backtest/scripts/*.py`、`backtest/report/*.py`。

尚未做(留給重構):`experiments/` 依 tag 分子資料夾、`ncku/out/` 舊產物壓縮、`TICKER_SETS` 搬到 `common/tickers.py`、`labels` / `metrics` 抽到 `common/`。

---

# 重整前的盤點(2026-09-12 上午,保留供對照)

## 一、頂層資料夾

| 資料夾 | 角色 | 狀態 | 備註 |
|---|---|---|---|
| `xgb_ta_pipeline/` | **主線**:約 240 個技術指標 → XGBoost → 重要性剪枝 | 使用中 | v3 模型來自這裡。8/7 前叫「老師的方法」 |
| `backtest_2026/` | **回測與比較工作區**:成大框架、跨方法比較、報告 | 使用中 | 所有 2026 樣本外結果與逐年回測 |
| `KLINE/fcukproject/` | 系統 A:K 線型態 AE + OC-SVM(Tkinter GUI),自己的 git repo | 診斷完畢,待重構 | `main` 分支;Chart-GCN 在同 repo 另一分支 |
| `chartgcn/` | 系統 B:Chart-GCN 論文復現,自己的 git repo | 已收尾(否證鏈完成) | **資料快取在這裡**,XGB 與回測都依賴它 |
| `tomt/Qmodel/` | 隊友 Tom 的方法(LightGBM 五模型) | 唯讀參考 | 不算我們的方法;`backtest_2026/tomt_port` 有移植版 |
| `tomt_backup_20260825/` | Tom 專案備份 | 可封存 | |
| `microsoft/` | Qlib 研究(含 `qlib-venv` 虛擬環境,很大) | 可封存 | 6/17 課堂報告用 |
| `sysmon/` | 系統監控小工具 | 與專題無關 | |
| `K/` | 舊資料夾,只有 `fcukproject` 殘留 | 可刪 | 確認與 `KLINE/fcukproject` 無差異後刪 |
| `進度整理/` | 週進度紀錄(md)+ `_build_progress.py` | 使用中 | |
| `系統流程架構報告*.pptx` | 三版簡報 | | |

## 二、各資料夾內部

### `xgb_ta_pipeline/`(主線模型)

```
src/
  run_pipeline.py     主入口:資料 → 特徵 → 標籤 → 切分 → 訓練 → 重要性 → 剪枝 → 評估
                      參數:--pool --horizon --label bin|cs --feature-sets --prune --sample-filter --chip --lag --stride
  data.py             讀 chartgcn/cache 的 yfinance 還原價(TW_CACHE_DIR 可切 cache_2026)
  features/           七個族群:trend momentum volatility volume candle stats event + cross_section + chip
  labels.py           r_h、y_bin_h(漲跌)、y_cs_h(贏過同池中位數)
  split.py            時間切分 + purge h 天
  train_xgb.py        XGB 參數與早停
  importance.py       gain / permutation / SHAP 合成排名、相關去重
  prune.py            cum90 / top20 / top40 / top80 / permpos
  evaluate.py         Acc/AUC/P/R/F1、acc_cov5/10/20/50、十分位多空(含成本)
  lowmem.py           tw500 用的 memmap 低記憶體路徑
  chip_data.py        FinMind 籌碼下載(FINMIND_TOKEN 環境變數)
  tune_xgb.py         超參數格點(結論:預設最好)
  plots.py, make_summary_fig.py
config/
  default.json        train ≤2022 / val 2023 / test 2024(2024 實驗用)
  y2021.json … y2026.json   逐年 walk-forward(train ≤Y-2、val Y-1、test Y)
  feature_catalog_*.csv     各池特徵清單
experiments/          308 個檔:<tag>.json(設定+結果)、<tag>_testpred.parquet(測試期機率)、
                      <tag>_importance.csv、features_<pool>_<start>_<end>.parquet(特徵快取,33 個,最大)
                      tag 規則:e*/x* 2024 實驗、x26-* 2026、x21..x25 逐年、vf* 量增篩選、diag* 診斷
data/finmind, finmind_2026   籌碼快取
figs/                 圖
實驗記錄.md、計畫書.md、README.md、tests/test_features.py
```

### `backtest_2026/`(回測工作區)

```
run_xgb_2026.py / run_cgcn_2026.py / run_kline_2026.py   三套方法各自口徑的 2026 回測(還原價、扣 0.585%)
predict_full.py / predict_full_tw50.py / vf_predict_full.py   重訓後對全部交易日打分(補沒有標籤的最後幾天)
compare_vf_acc.py / compare_vf_v3_acc.py                 模型準確率比較(隔天 vs 5 日、量增篩選)
run_kline_sup_2026.py / run_kline_plus_ta.py / run_kline_classic4.py   KLINE 有監督版、加四指標
portfolio.py, make_report.py, build_html.py, build_final_html.py, final_template.html   報告
data/kline_raw/       KLINE 用的 yfinance 未還原+還原價
results/              38 個檔:*_2026.json、*_testpred/fullpred.parquet、final_best.json/html、vf_*、kline_*
tomt_port/tomt_walkforward.py   Tom 五模型逐年重訓(直接 import tomt/Qmodel/src)
ncku/                 成大 stock_backtest_v1 框架工作副本(正式口徑)
  backtest/           框架本體(已 patch get_stock_information、rolling 問題繞過)
  run_ncku.py         框架包裝:Strategy 基類、assemble_csv、BuyHold、run()
  run_logic.py        v1 量價邏輯(180 組);load_indicators
  run_logic2.py       Logic2 策略類(流動性/大盤/汰弱留強/停損/limit_aware)+ run_one(成本估算)★ v3 核心
  run_window.py       相位敏感度、--freq 每日
  run_daily_grid.py / run_hold_rules.py / run_maxprofit.py / run_optimize.py   交易規則格點
  run_pool_scan.py / run_pools_v3.py   換股票池
  run_years.py        v3 逐年 2021–2026 ★
  run_vf_years.py / run_vf_v3.py       量增篩選模型逐年
  run_tomt.py         Tom 規則逐年
  prep_final_v3.py / prep_final_report.py   報告資料
  fetch_official.py / update_official.py / make_yf_src.py   資料抓取與轉換
  tmp/official/       證交所官方資料 2025-10 ~ 2026-09-11(NCKU_SRC=official,正式口徑)
  tmp/yf/, tmp/yf_hist/   yfinance 還原價轉框架格式(yf_hist 2020-09 ~ 2026-09-10,逐年回測用)
  out/                每次回測的 xlsx + summary_*.json(約 30 個 summary)
```

### `KLINE/fcukproject/`(系統 A)

```
main.py                       GUI 入口
kline_pattern_search.py       舊入口(向後相容層)
kline/
  config.py      參數:HOLD 5、±5%、DIST 3、LATENT 4、EPOCHS 60、nu 0.1、訓練 2016–2024、測試 2025、TW50 清單
  features.py    1 日 7 / 2 日 10 / 3 日 12 特徵
  data_loader.py yfinance 下載、build_dataset
  autoencoder.py AE(input → 2×input → 8 → 4)
  pattern_model.py  歐氏距離找種子群 → 重要性 → AE(只用型態樣本)→ OC-SVM(只用正例)★ 壞在這
  validate.py    ② 特徵驗證(80/20,P/R/Acc/F1)
  backtest.py    非重疊持倉回測
  gui.py
  supervised_model.py   ★ 新增:OC-SVM → 有反例分類器(logistic/xgb),AE 可留可不留
```

### `chartgcn/`(系統 B + 共用資料)

```
core/              data_loader.py(TICKER_SETS:tw50/100/200/500/electronics/semi/elec_*/tw50_elec ★ 所有專案共用)
                   dataset / model / train / pip_algorithm / subgraph / vg_graph / events / indicators
analysis/          decile_ls.py(十分位評估,被 xgb evaluate.py 移植)、knn、bootstrap、審計文件
cache/             ★ yfinance 還原價主快取(1,779 檔,2016–2024 實驗用)
cache_2026/        ★ 2026 用快取(816 檔;TW_CACHE_DIR 指到這裡)
experiments_2026 / experiments_paper / experiments_fusion   Chart-GCN 實驗(267 檔)
backtest.py, trend_signal.py, 725/, chinatest/, papers/, figs/, logs/
```

## 三、跨資料夾依賴(重構時要注意)

```
chartgcn/core/data_loader.py  ──TICKER_SETS、讀快取──▶  xgb_ta_pipeline/src/data.py
chartgcn/cache*, cache_2026   ──還原價──────────────▶  xgb 特徵、backtest_2026 全部、ncku/make_yf_src.py
chartgcn/analysis/decile_ls   ──移植──────────────▶  xgb_ta_pipeline/src/evaluate.py
xgb_ta_pipeline/experiments   ──testpred/json─────▶  backtest_2026/*、ncku/run_years.py、run_vf_*.py
xgb_ta_pipeline/src           ──import split/train_xgb──▶ backtest_2026/predict_full*.py、vf_predict_full.py
KLINE/fcukproject/kline       ──import────────────▶  backtest_2026/run_kline_*.py
tomt/Qmodel/src               ──import 特徵程式──▶  backtest_2026/tomt_port/tomt_walkforward.py
ncku/run_ncku.py + run_logic2.py ──Strategy/Logic2/run_one──▶ ncku/run_*.py 全部
```

三個 sys.path 注入點:`backtest_2026/*.py` 都用 `sys.path.insert` 拉 `../xgb_ta_pipeline/src`、`../KLINE/fcukproject`、`../chartgcn/core`。重構時建議統一成一個套件或環境變數。

## 四、資料快取一覽

| 位置 | 內容 | 大小/數量 | 誰在用 |
|---|---|---|---|
| `chartgcn/cache/` | yfinance 還原價,多個區間版本 | 1,779 檔 | 2024 實驗 |
| `chartgcn/cache_2026/` | yfinance 還原價到 2026-09 | 816 檔 | 2026 全部 |
| `xgb_ta_pipeline/experiments/features_*.parquet` | 算好的特徵面板 | 33 檔(最大的幾百 MB) | run_pipeline、predict_full |
| `xgb_ta_pipeline/data/finmind*` | FinMind 籌碼 | tw500 | --chip(結論沒用) |
| `backtest_2026/ncku/tmp/official/` | 證交所官方日資料 | 251 檔,2025-10 起 | 正式口徑回測 |
| `backtest_2026/ncku/tmp/yf_hist/` | 還原價轉框架格式 | 217 檔,2020-09 起 | 逐年回測 |
| `backtest_2026/data/kline_raw/` | KLINE 用 yfinance | 50 檔 | KLINE |
| `microsoft/qlib-venv/` | 虛擬環境 | 很大 | 無 |

## 五、重構前可以先做的整理

1. **封存**:`tomt_backup_20260825/`、`microsoft/`、`K/`、`sysmon/` 移出專題目錄或壓縮。
2. **實驗產物分層**:`xgb_ta_pipeline/experiments/` 308 檔混在一起,建議依 tag 前綴分子資料夾(`2024/ e*,x*`、`2026/ x26-*`、`years/ x21..x25`、`vf/`、`diag/`),`features_*.parquet` 獨立到 `cache/`。
3. **ncku/out**:30 個 summary_*.json 與 60 個 xlsx 資料夾,保留 `summary_years.json`、`summary_vf_*.json`、`summary_logic2.json`、`final_best/`,其餘可壓縮。
4. **共用模組獨立**:`TICKER_SETS`、快取讀取、標籤定義、評估指標(P/R/Acc/F1 + 十分位)目前分散在三個 repo,重構時抽成一個 `common/` 套件,三條線都 import 它。
5. **KLINE 重構的落點**:建議另開 `kline_v2/`(或 `pattern_ae/`),用 `common/` 的資料與評估,不動 `KLINE/fcukproject`(GUI 版保留當歷史)。
6. **文件**:每條線的「結論」目前在 `xgb_ta_pipeline/實驗記錄.md`、`chartgcn/analysis/audit_*.md`、`backtest_2026/results/summary_*.md` 和 `進度整理/`,重構後統一到 `進度整理/`。
